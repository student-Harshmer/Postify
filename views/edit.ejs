<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/Stylesheets/output.css" rel="stylesheet">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <h5>Edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind ?" class="w-1/4 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-5" name="content" id=""><%= post.content %></textarea>
            <input class="px-3 py-2 w-40 bg-yellow-500 text-black rounded-md mt-2.5 block cursor-pointer" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>